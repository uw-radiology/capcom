import './style.css';
import {html, render} from 'lit-html';
import { service } from 'image-service.js';

// A lit-html template uses the `html` template tag:
let body = () => html`

`;




render(body(), document.body);